#include"main.h"

/**
 * more_numbers - print 0 - 14 ten times and you
 *
 * Return:  0
 */

void more_numbers(void)
{
	int x, r, c;

	for (r = 1; r < 11; ++r)
	{
		for (c = 0; c <= 14; ++c)
		{
			x = c;
			if (c > 9)
			{
				_putchar(1 + 48);
				x = c % 10;
			}
			_putchar(n + 48);
		}
		_putchar('\n');
	}
}
